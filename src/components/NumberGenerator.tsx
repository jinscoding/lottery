import styled from "styled-components";

function NumberGenerator() {
  return (
    <NumberGeneratorStyle>
      <h1>NumberGenerator</h1>
    </NumberGeneratorStyle>
  );
}

const NumberGeneratorStyle = styled.div``;

export default NumberGenerator;
